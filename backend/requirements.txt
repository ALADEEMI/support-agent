# Backend requirements.
# NOTE: keep this file ASCII-only. pip decodes requirement files using the
# system locale codepage on Windows, so non-ASCII characters break parsing.

Flask==3.1.3
flask-cors==6.0.5
python-dotenv==1.2.3

langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1

tensorflow==2.21.0
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.1

pytest==9.1.1
