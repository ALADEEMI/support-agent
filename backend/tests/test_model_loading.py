"""اختبار تحميل الموديل والتحقق من تنبؤ منطقي (sanity check) قبل ربطه بأي شيء.

حرج: يتأكد من نجاح تحميل `model.h5` + `tokenizer.pkl` + `label_encoder.pkl`
من `backend/model/artifacts/` ومن أن التطبيع مطابق للتدريب (ADR قسم 6.1).

يُبنى في Phase 4، ولا يُستخدم أي mock إذا لم تكن ملفات الموديل موجودة — يتوقف التنفيذ ويُطلب تزويدها.
"""
