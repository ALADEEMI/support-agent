# سجل تقدم التنفيذ (PROGRESS)

مرجع التنفيذ: `docs/ADR.md` (قسم 10 للمراحل، قسم 11 للتوثيق).

## حالة المراحل

- [x] Phase 0 — الإعداد الأساسي (Bootstrap) — 2026-09-11 — هيكل كامل + بيئة Python 3.12 + تثبيت الاعتماديات + فحص الاستيراد/health. **بانتظار إذن صريح للانتقال إلى Phase 1.**
- [ ] Phase 1 — توليد وتنظيف الداتا سيت
- [ ] Phase 2 — تدريب وتقييم نموذج RNN
- [ ] Phase 3 — قاعدة البيانات
- [ ] Phase 4 — طبقة الوكيل (Extractor + Tools + Graph)
- [ ] Phase 5 — Flask Backend
- [ ] Phase 6 — React Frontend
- [ ] Phase 7 — اختبار شامل وتوثيق نهائي

## قرارات إضافية أثناء التنفيذ

1. **بيئة Python 3.12 بدل 3.14**: النظام الافتراضي فيه Python 3.14.7، لكن `tensorflow` لا يوفر أي حزمة (wheel) لإصدار 3.14 (`from versions: none`). أُنشئت البيئة الافتراضية `.venv` عبر `py -3.12` (TensorFlow 2.21.0).
2. **تثبيت إصدارات الاعتماديات** في `backend/requirements.txt` على النسخ التي ثُبِّتت فعلياً، لضمان قابلية إعادة الإنتاج.
3. **`backend/requirements.txt` يبقى ASCII فقط**: اكتُشف أثناء Phase 0 أن pip يفك ترميز ملف المتطلبات بترميز النظام (cp1252 على Windows)، فتعليق عربي داخل الملف أسقط التثبيت بـ `UnicodeDecodeError`. أُزيلت التعليقات العربية وأُبقي الملف ASCII.
4. **`backend/config.py`**: لا قيمة افتراضية لـ `COMMANDCODE_API_KEY` إطلاقاً؛ و`require_api_key()` يرفع `RuntimeError` برسالة عربية واضحة عند غيابه (بلا fallback صامت). بقية المسارات لها قيم افتراضية مطابقة لـ `.env.example`.
5. **نقل `ADR.md` إلى `docs/ADR.md`** حسب قسم 2 من الوثيقة. تبيّن أن الملف كان متتبعاً بـ git أصلاً في `docs/ADR.md`؛ نُسخ محتوى نسخة الجذر فوقه (متطابقان، 421 سطراً) وحُذفت نسخة الجذر.
6. **الواجهة الأمامية تعتمد Create React App** (`react-scripts` 5.0.1) لأن ADR يستخدم `REACT_APP_API_URL`.
7. **إضافة `backend/tests/test_model_loading.py`** رغم أن شجرة قسم 2 لم تذكره، لأنه مطلوب نصاً في قسم 6.1 (نقطة 5).
8. **لم أُنشئ `docs/AGENT_KICKOFF_PROMPT.md`** لأن محتواه غير متوفر بالوثيقة، ولم أخترعه.
9. **git**: المستودع كان موجوداً مسبقاً (commit أول `18d30e0` + remote `origin` إلى `github.com/ALADEEMI/support-agent`). نُفِّذ `git init` فقط، ولم يُعمل أي `commit` ولا `push`.

## قضايا مفتوحة / مخاطر

1. **فجوة في ADR**: قسم 8.2 يحيل إلى "الرسم بالنقاش السابق" لتسلسل عقد LangGraph، وهذا الرسم غير مضمّن في الوثيقة. تسلسل العقد/الحواف الشرطية غير معرّف — **مطلوب قبل Phase 4**.
2. **فجوة في ADR**: قسم 5.1 لا يحدد الداتا سيت الإنجليزي المصدر (اسم/رابط/ملف) — **مطلوب قبل Phase 1**.
3. **فجوة في ADR**: مزوّد الـ LLM والـ endpoint والموديل الحقيقي غير محددين (`LLM_MODEL` مجرد "مثال" في قسم 3) — مطلوب قبل Phase 4/5.
4. **ملف غير متوقع**: `training/raw_dataset/raw_dataset.csv` (حجم ~1.19MB، 4872 صفاً بيانات، أعمدة `text,category,product_name,order_id`) ظهر أثناء الجلسة ولم أنشئه، وغير متتبع بـ git. لم أعدّله ولم أحذفه. مصدره غير مؤكد.
5. **مخاطرة توافق**: يجب أن يتوافق إصدار TensorFlow/Keras محلياً مع بيئة Colab المستخدمة في التدريب عند تحميل `model.h5`/`.keras` (Phase 2 و 4). TensorFlow 2.21 يستخدم Keras 3.
6. **مخاطرة Phase 6**: Create React App مهجور وقد يتعارض مع Node 24 المثبّت على الجهاز.

## سجل التحديثات

- 2026-09-11 — إنشاء الملف.
- 2026-09-11 — اكتمال Phase 0 وتسجيل القرارات والمخاطر. بانتظار الإذن للانتقال إلى Phase 1.
