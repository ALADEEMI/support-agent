# سجل تقدم التنفيذ (PROGRESS)

مرجع التنفيذ: `docs/ADR.md` (قسم 10 للمراحل، قسم 11 للتوثيق).

## حالة المراحل

- [x] Phase 0 — الإعداد الأساسي (Bootstrap) — 2026-09-11 — هيكل كامل + بيئة Python 3.12 + تثبيت الاعتماديات + فحص الاستيراد/health + commit `746929e`.
- [x] Phase 1 — توليد وتنظيف الداتا سيت — 2026-09-11 — الداتا سيت (4000 صف) مُولَّد على Colab بواسطة صاحب المشروع، وتم **التحقق الفعلي** من معايير الجودة (Gate 1 PASS، Gate 2 PASS، مراجعة يدوية 100 صف) وإنتاج `dataset_summary.md` و `generation_log.md`.
- [x] Phase 2 — تدريب وتقييم نموذج RNN — 2026-09-11 — اعتماد مخرجات التدريب (`BiLSTM_Baseline`، Accuracy 0.9450، Macro F1 0.9449) دون إعادة تدريب، مع `model_comparison.md` واختبار تحميل و Sanity Check ناجح (5/5). commit `d70ed23`.
- [x] Phase 3 — قاعدة البيانات — 2026-09-11 — `schema.sql` (6 جداول + 3 فهارس)، `db.py`، `seed_data.py` (6 عملاء، 15 منتجاً، 15 طلباً، 3 سياسات). تحقق فعلي من الجداول والقيود وأمان إعادة التشغيل. commit `42e2c40`.
- [x] Phase 4 — طبقة الوكيل (Extractor + Tools + Graph) — 2026-09-11 — 6 عقد + حواف شرطية + `SqliteSaver`، **45/45 اختبار ناجح**، **و6 سيناريوهات نُفِّذت فعلياً عبر سكربت بايثون مباشر بنموذج RNN وقاعدة بيانات و LLM حقيقيين**، ثم إصلاح انحراف اللهجة وإعادة اختبار السيناريوهات الحساسة (كل الردود صارت يمنية). **بانتظار إذن صريح للانتقال إلى Phase 5.**
- [x] Phase 5 — Flask Backend — 2026-09-11 — `POST /api/chat` + `/health` + CORS، ربط الـ Graph، وحفظ كل رسالة في `chat_messages`. **18 اختبار API (المجموع 68/68 ناجح)**، واختبار HTTP حقيقي بـ 3 رسائل متتالية. أُصلحت فجوة استهلاك الذاكرة (خيار 2+3) و**أُعيد التحقق فعلياً: الدورة الثانية استرجعت `order_id=1002` بلا إعادة ذكره**. **بانتظار إذن صريح للانتقال إلى Phase 6.**
- [x] Phase 6 — React Frontend — 2026-09-11 — واجهة محادثة كاملة + `GET /api/history/<session_id>`، **71/71 اختبار**، و**اختبار واجهة حقيقي في متصفح Chrome** (4 رسائل من داخل الواجهة ← 8 فقاعات، ثم إعادة تحميل الصفحة ← السجل يُعاد بناؤه بالكامل `history_matches: true`). ثم **إصلاحان بمراجعة صاحب المشروع**: (أ) حظر تنسيق Markdown في `SYSTEM_PROMPT` (كانت النجوم `**` تظهر حرفياً في الفقاعات)، (ب) إعادة تصميم بصري كامل (كحلي/ذهبي) بلا أي مكتبة جديدة. لقطة الشاشة: `docs/phase6_ui_screenshot.png`. **بانتظار إذن صريح للانتقال إلى Phase 7.**
  - `api.js`: `sendMessage` + `fetchHistory`. `ChatWindow.jsx`: رسائل + إدخال + تحميل السجل عند الفتح + معرّف جلسة ثابت في `localStorage`.
  - ملفات CRA الإلزامية المضافة (غير مذكورة في ADR قسم 2): `frontend/src/index.js`، `frontend/src/index.css`، `frontend/public/index.html`، و`frontend/src/components/ChatWindow.css`.
  - تحقق فعلي: `npm install` (856 حزمة)، `npm run build` → **Compiled successfully** على Node 24، وسيرفر التطوير يخدم الصفحة.
  - تفاصيل كاملة وسجل المحادثة في `docs/phase6_ui_test_transcript.md`.
- [x] Phase 7 — اختبار شامل (End-to-End) وتوثيق نهائي — 2026-09-11 — **10/10 سيناريوهات E2E ناجحة** عبر السيرفر والنموذج وقاعدة البيانات والـ LLM الحقيقيين (كل الفئات الأربع + طلب غير موجود + رسالة غامضة + تصعيد بتذكرة + ذاكرة متعددة الدورات + حارس الاختلاق). المجموع الكلي: **71/71 اختبار وحدة**. أُنتج `docs/phase7_e2e_report.md` و`docs/PROJECT_REPORT.md`، وحُدِّث `README.md` النهائي. **المشروع مكتمل وجاهز للعرض أمام اللجنة.**

## قرارات إضافية أثناء التنفيذ

1. **بيئة Python 3.12 بدل 3.14**: النظام الافتراضي 3.14.7، لكن `tensorflow` لا يوفر أي حزمة لإصدار 3.14. أُنشئت البيئة `.venv` عبر `py -3.12` (TensorFlow 2.21.0).
2. **تثبيت إصدارات الاعتماديات** في `backend/requirements.txt` على النسخ المثبّتة فعلياً.
3. **`backend/requirements.txt` ASCII فقط**: pip يفك ترميز ملف المتطلبات بترميز النظام (cp1252)، فتعليق عربي داخل الملف أسقط التثبيت.
4. **`backend/config.py`**: لا قيمة افتراضية لـ `COMMANDCODE_API_KEY`؛ `require_api_key()` يرفع `RuntimeError` واضحاً عند غيابه.
5. **نقل `ADR.md` إلى `docs/ADR.md`** حسب قسم 2.
6. **الواجهة الأمامية Create React App** (`react-scripts` 5.0.1) لأن ADR يستخدم `REACT_APP_API_URL`.
7. **Phase 1 — النوتبوك مصدر الحقيقة**: لا اعتماد على أي داتا سيت إنجليزي خارجي (يلغي وصف قسم 5.1).
8. **Phase 1 — التحقق لا التوليد**: لم يُعَد توليد الداتا؛ تشغيل فحوصات الجودة فقط.
9. **Phase 1 — سكربتات التحقق** نُفِّذت من مجلد الـ scratchpad وليس كملفات مشروع.
10. **Phase 2 — اعتماد المخرجات الجاهزة دون إعادة تدريب** (قرار صاحب المشروع).
11. **Phase 2 — استكمال `normalizer.py` مبكراً** لأنه مطلوب لاختبار تطابق التطبيع (ADR 6.1).
12. **Phase 2 — إضافة `MODEL_CONFIG_PATH`** (معتمد من صاحب المشروع).
13. **Phase 2 — توثيق `model_comparison.md`**.
14. **تحديث ADR (بموافقة صاحب المشروع)**: `model.keras` + `config.json`، وانقضاء قرار قائمة المنتجات، وبنية الـ Graph في 8.2.
15. **Phase 3 — `DATABASE_PATH` النسبي يُحلّ بالنسبة لمجلد `backend/`**.
16. **Phase 3 — اتصال SQLite لكل نداء** (`try/finally` + `PRAGMA foreign_keys = ON`).
17. **Phase 3 — `seed_data.py` حتمي وقابل لإعادة التشغيل** (تحقق فعلي بتشغيله مرتين).
18. **Phase 3 — `*.db` مُستثنى من git** (معتمد).
19. **Phase 3 — أرقام الطلبات في البذور صريحة (1001–1015)** بدل AUTOINCREMENT.
20. **Phase 3 — `.commandcode/` أُلغي تتبعه** (`git rm -r --cached`) مع بقاء الملفات محلياً (معتمد).
21. **Phase 4 — `SqliteSaver` بالمُنشئ لا `from_conn_string`** (معتمد من صاحب المشروع): في `langgraph-checkpoint-sqlite==3.1.1` تُرجع `from_conn_string(...)` مُدير سياق لا كائناً جاهزاً، فصيغة ADR الأصلية لا تعمل. **حُدِّث كود ADR قسم 8.3 ليطابق التطبيق الفعلي.** ملف الذاكرة: `backend/database/agent_memory.db`.
22. **Phase 4 — إضافة `COMMANDCODE_API_URL`** إلى `config.py` و `.env.example` بقيمة افتراضية مطابقة للنوتبوك.
23. **Phase 4 — استدعاء الـ LLM داخل `graph.py`** (`call_llm`) بدل إضافة ملف غير مذكور في ADR قسم 2.
24. **Phase 4 — استبدال استدعاء الـ LLM في اختبارات الوحدة** عبر monkeypatch على `graph.call_llm` (اعتماد شبكي خارجي فقط؛ ملفات الموديل حقيقية دائماً).
25. **Phase 4 — النص الأصلي يُمرَّر للـ LLM لا المطبَّع**: التطبيع للنموذج فقط، لأن التطبيع يشوّه الإملاء.
26. **Phase 4 — تصحيح مسار الشكوى** (معتمد من صاحب المشروع): `complaint → extract_info → escalate_ticket` بدل تخطّي الاستخراج، ليُخزَّن `order_id` على التذكرة إن ذكره العميل. حُدِّث `graph.py` و ADR قسم 8.2 وأُضيف سيناريو اختبار.
27. **Phase 4 — `config.py` يحلّ مساراته من موقعه هو لا من `cwd`** (معتمد من صاحب المشروع):
    - `.env` يُحمَّل من **جذر المشروع** (`BACKEND_DIR.parent / ".env"`) حسب ADR قسم 2.
    - كل المتغيرات المسارية (`DATABASE_PATH`, `RNN_MODEL_PATH`, `TOKENIZER_PATH`, `LABEL_ENCODER_PATH`, `MODEL_CONFIG_PATH`) تُفسَّر نسبةً إلى **`backend/`** وتُخزَّن كمسارات **مطلقة**.
    - مُتحقَّق منه بـ `backend/tests/test_config.py` بما فيه تشغيل في عملية فرعية من مجلد مختلف (`cwd` آخر).
28. **Phase 4 — إصلاح انحراف اللهجة عبر البرومبت فقط** (معتمد من صاحب المشروع): أُضيف إلى `SYSTEM_PROMPT` سطر منع صريح للهجات المصرية/الشامية + مثالان قصيران (few-shot) باللهجة اليمنية. **لم يُغيَّر `temperature`** عن قصد، لعزل أثر المتغيّر الواحد. حُدِّث ADR قسم 9 ليطابق التنفيذ، وحُفظ سجل قبل/بعد في `docs/phase4_dialect_retest.md`.
29. **Phase 5 — تفعيل CORS (`flask-cors`)**: متطلب عملي لاستدعاء React للـ backend من أصل (origin) مختلف. غير منصوص عليه في ADR لكنه ضروري للقسم 1. المتطلبات كانت تحتوي `flask-cors` مسبقاً.
30. **Phase 5 — خريطة أخطاء HTTP**: 400 لخطأ المدخلات، 500 لغياب المفتاح/خطأ داخلي، 502 لفشل مزوّد الـ LLM — كلها بصيغة `{"error": "..."}` موحدة (لا صفحات HTML). ورسالة العميل تُحفظ في `chat_messages` **قبل** تشغيل الوكيل، فتبقى محفوظة حتى لو فشل الـ LLM.
31. **Phase 5 — `POST /api/chat` فقط**: `GET /api/history/<session_id>` أُجِّل إلى Phase 6 كما ينص ADR قسم 10 (دالة `db.get_chat_history` جاهزة له).
32. **Phase 5 — إصلاح استهلاك الذاكرة (خيار 2 + 3 معاً، معتمد من صاحب المشروع)**:
    - **الإصلاح الجذري:** `run_turn` يفحص وجود حالة سابقة عبر `graph.get_state(config).values` (مُتحقَّق فعلياً أن جلسة غير معروفة تُرجع `{}`). الدورة الأولى تُمرَّر بـ `initial_state` الكاملة، والدورات اللاحقة تُمرَّر بـ `turn_input` (حقلان فقط) فيُستَرجع الباقي من `SqliteSaver` (ADR قسم 8.3).
    - **`order_id` ملتصق:** `extract_info` لا يطمس `order_id` بـ `None` إذا لم تذكر الرسالة الجديدة رقماً؛ يُستبدل فقط برقم جديد. موثّق في docstring العقدة وفي `state.py`.
    - **إضافة ضرورية (لم تُطلب صراحةً لكنها لازمة للصحة):** `classify_message` تُصفِّر `tool_result` و `needs_escalation` في كل دورة، لأنها أول عقدة في كل مسار. بدون هذا كان يمكن أن يتسرّب استعلام طلب قديم إلى ردّ لا علاقة له به، ويبقى التصعيد `true` في دورة ليست شكوى. أُضيف لها اختباران مستقلان.
    - **لم يُنفَّذ** تمرير سجل المحادثة الكامل إلى سياق الـ LLM (خارج النطاق، بقرار صاحب المشروع) — قسم 9 كما هو.
    - **`product_name` ليس ملتصقاً** حسب القرار المعتمد (عدم تناظر مقصود مع `order_id`).
33. **Phase 5 — حظر اختلاق الأرقام/المدد/التواريخ (معتمد من صاحب المشروع)**: أُضيف إلى `SYSTEM_PROMPT` سطر صريح يمنع ذكر أي رقم/مدة/تاريخ/تفصيل سياسة ما لم يرد **حرفياً** في `tool_result`، مع مثال ثالث (few-shot) يُظهر الرفض الصريح عند غياب المعلومة. حُدِّث ADR قسم 9، وحُفظ سجل قبل/بعد في `docs/phase5_grounding_retest.md`. **`temperature` لم يُغيَّر.** هذا حارس مستقل عن إصلاح دقة التصنيف (يظل نافعاً لأي حالة يغيب فيها نتيجة الأداة مستقبلاً).
34. **Phase 5 — توثيق سلوك الحالة في ADR قسمي 8.1 و 8.2** (معتمد من صاحب المشروع): «`order_id` ملتصق»، و«`product_name` غير ملتصق»، و«`tool_result`/`needs_escalation` لكل دورة»، و`turn_input` في الدورات اللاحقة.
35. **Phase 6 — التحقق البصري عبر Chrome الموجود + CDP فقط** (قرار صاحب المشروع: عدم تثبيت أي أداة عامة جديدة): استُخدم Chrome المثبّت مسبقاً مع `--headless=new --remote-debugging-port`، وقاد الاختبارَ سكربت Node يستعمل `WebSocket`/`fetch` المدمجَين في Node — **بلا أي اعتماد جديد**. السكربت في مجلد الـ scratchpad (ليس ملف مشروع).
36. **Phase 6 — إصلاح سباق زمني حقيقي في `ChatWindow.jsx`** (اكتُشف بالاختبار الفعلي): طلب تحميل السجل عند فتح الصفحة كان يستدعي `setMessages(history)` مباشرة، فإذا تأخّر (بدء بارد) ووصلت نتيجته **الفارغة** بعد أن أضاف المستخدم رسالته، محت فقاعة العميل. الإصلاح: لا يُستبدل السجل إلا إذا بقيت القائمة فارغة `previous.length === 0 ? history : previous`، مع علم `cancelled` لتفادي تحديث مكوّن أُزيل. أُثبت السبب بـ `MutationObserver` على حاوية الرسائل (التسلسل موثّق في `docs/phase6_ui_test_transcript.md`).
37. **Phase 6 — تجاهل `build/` في `.gitignore`**: ناتج `npm run build` مُولَّد ولا يُرفع.
38. **Phase 6 — حظر تنسيق Markdown في `SYSTEM_PROMPT`** (بمراجعة صاحب المشروع): كانت ردود الوكيل تحتوي نجوماً حرفية (`**14 يوم**`) تظهر كما هي في الفقاعات لأن الواجهة لا تعرض Markdown. الحل **عند المصدر**: سطر صريح في `SYSTEM_PROMPT` يمنع النجوم/الشرطات السفلية/الاقتباس المائل/القوائم/العناوين (محادثة واتساب لا تعرض تنسيقاً). **لم تُضَف أي منطق تحليل Markdown في الواجهة** (قرار صاحب المشروع: إصلاح البرومبت أبسط وأصح). حُدِّث ADR قسم 9. **التحقق:** 8 رسائل حقيقية، **0 منها تحتوي أي رمز Markdown** (فحص فعلي على النص الخام المحفوظ في `chat_messages`).
39. **Phase 6 — إعادة تصميم بصري (CSS/JSX فقط، بلا مكتبات)**: ملف جديد `frontend/src/components/ChatWindow.css` (الكحلي `#1B2A4A` أساسي والذهبي `#C9A961` لون مميز)، خلفية متدرجة متحركة عبر `@keyframes`، فقاعات بزوايا أنعم وظلال، صورة رمزية لكل جهة (أيقونة عميل SVG مضمّنة + حرف «ن» للوكيل)، ظهور تدريجي للرسائل (`bubbleIn`)، مؤشر كتابة متحرك، شعار وحالة اتصال في الترويسة، ووقت مختصر تحت كل فقاعة. `index.css` اختُصر إلى تهيئة أساسية لتفادي تعارض الأنماط القديمة.
   - **ملاحظة قرار صغير:** نص مؤشر الكتابة نُفِّذ «سارة تكتب…» بدل «…يكتب» لأن الوكيلة أنثى (سارة) والصيغة المذكّرة تبدو خطأً نحوياً. قابل للتغيير بطلب صاحب المشروع.
   - **الوقت تحت الفقاعة:** الرسائل الجديدة تأخذ وقتاً محلياً، والرسائل المُحمَّلة من السجل تأخذ وقتها من `chat_messages` — بنفس الصيغة `YYYY-MM-DD HH:MM:SS`.
40. **Phase 6 — إصلاح تباين منطقة المحادثة (بمراجعة صاحب المشروع، بتشخيص قائم على قياس فعلي)**:
   - **التشخيص الصحيح:** لم يكن هناك خلل في تطبيق الأنماط — القياس أظهر أن `.chat-window` فعلاً كريمي (`rgb(247,245,240)`) و`.chat-messages` شفاف يرثه. المشكلة كانت **في التصميم**: فقاعات الوكيل الكحلية بعرض 76% ونصوص طويلة كانت تغطي معظم السطح الفاتح، فتبقى الكريمي هوامش رفيعة فقط → المنطقة تُقرأ ككحلية بالكامل. (كان أول تقرير لي معتمداً على وصف أداة الرؤية، وقد كان **مضلِّلاً** — الاعتماد صار على **فك ترميز بكسلات اللقطة** وقياس نسب الإضاءة.)
   - **الإصلاح:** خلفية فاتحة صريحة لمنطقة الرسائل (`#faf7f0`) + **نقل التدرّج المتحرك ليصير داخل منطقة المحادثة الفاتحة** (`@keyframes chatShimmer` على `.chat-messages`) بدل أن يكون طبقة كحلية على الصفحة (الصفحة صارت إطاراً كحلياً ثابتاً)، وتضييق الفقاعات إلى 68% وتخفيف الحشو، وتقوية حدود فقاعة العميل.
   - **التحقق العددي بعد الإصلاح:** منطقة المحادثة = **47.5% فاتح / 50.7% غامق** (لم تعد كحلية بالكامل)، و`messages_bg_right = #f7f2e7` و`messages_bg_left = #f0e7d5` (سطوح فاتحة واضحة)، مع بقاء فقاعات الوكيل كحلية بنص فاتح كما اعتمد صاحب المشروع.
   - **ملاحظة صريحة:** ما زال نحو نصف المساحة كحلياً لأن ردود الوكيل طويلة وفقاعاته كحلية بقرار معتمد. إن أراد صاحب المشروع مزيداً من الإشراق فالخيار المتاح هو تفتيح تعبئة فقاعة الوكيل (كحلي أفتح) — لم يُنفَّذ لأنه يخالف قرار «تبقى الفقاعة كحلية».
41. **Phase 7 — منهج التحقق**: 10 سيناريوهات E2E نُفِّذت عبر HTTP على سيرفر Flask حقيقي (لا عبر عميل اختبار)، وكل سيناريو يفحص: رمز HTTP، اكتمال السجل الدائم، خلوّ الرد من رموز Markdown، خلوّه من المفردات المصرية/الشامية، ومطابقة الفئة/رقم الطلب/التصعيد. النتائج محفوظة في `docs/phase7_e2e_report.md`. **لم يُضَف ملف اختبار جديد** التزاماً بأن «Phase 7 لا بناء جديد»، مع أن السيناريوهات موثّقة وقابلة لإعادة التنفيذ بأمر واحد.
42. **Phase 7 — قياس ثبات اللهجة آلياً بقيود**: فحص المفردات الممنوعة يعتمد على قائمة كلمات، وقد **فوّت** كلمة «نورت» (شامية/مصرية) في السيناريو 5. القيد موثّق في تقرير Phase 7 بدل الادّعاء بأن اللهجة سليمة 100%. (أُصلحت الكلمة نفسها لاحقاً — انظر البند 44.)
43. **Phase 7 (إغلاق) — توسيع حارس عدم الاختراع ليشمل الأفعال** (معتمد من صاحب المشروع): لم يعد مسموحاً ادّعاء أي إجراء (تسجيل/حفظ/رفع/تحويل) بلا تأكيد من `tool_result`. تطلّب ذلك **إصلاح تناقض داخلي** في نفس البرومبت: بند الشكوى كان يأمر بادّعاء التسجيل بلا شرط، فأُعيدت صياغته ليكون مشروطاً بإنشاء تذكرة فعلية. حُدِّث `prompts.py` و ADR قسم 9.
44. **Phase 7 (إغلاق) — إضافة «نورت» لقائمة المنع**: في `SYSTEM_PROMPT` و ADR قسم 9 وفي الفحص الآلي، مع **فحص ذاتي للمُكتشِف** (self-test) يثبت أنه يلتقط الكلمة فعلاً — حتى لا تكون النتيجة النظيفة ناتجة عن فحص معطّل.
45. **بعد Phase 7 — سكربت تشغيل بضغطة واحدة `run_project.bat`** (طلب صاحب المشروع، لا يضيف أي مرحلة جديدة): يتحقق من `python`/`node`/`npm`، وينشئ `.env` **فقط إن كان مفقوداً** (لا يستبدل موجوداً)، ويستخدم/ينشئ `.venv` (يفضّل `py -3.12`)، ويثبّت الاعتماديات عند نقصها، وينفّذ `npm install` عند الحاجة، ويعبّئ القاعدة عند غيابها، ثم يشغّل الخدمتين في نافذتين منفصلتين بعنوانين واضحين ويفتح المتصفح، ويطبع ملخصاً. **وضع `/dryrun`** أُضيف لتمكين اختبار كل الفحوصات دون تشغيل أي خدمة (وهو ما استُخدم في التحقق). كل المسارات مشتقة من `%~dp0`. لم تُضَف أي اعتماديات، ولا تُنشأ أي ملفات وقت التشغيل.

## إصلاح المعمار (Architecture Fix) — بعد الاختبار الحيّ متعدد الدورات

اكتُشفت أربع علل في تشغيل حيّ حقيقي، وأُصلحت جذرياً. التفاصيل المعمارية الملزمة في
`docs/ADR.md` قسم 8.1 و 8.2 و 9.

46. **Task 1 — عتبة ثقة النية:** `config.INTENT_CONFIDENCE_THRESHOLD = 0.65`. في `classify_message`
    يُحوَّل أي تصنيف تحت العتبة إلى `other` (مع تسجيل `intent_confidence` و`low_confidence`)،
    فلا تصل الرسالة إلى `fetch_order_status` ولا `escalate_ticket`. **الاستثناء:** وجود دليل
    نصي قاطع (كلمة سياسة، طلب تصعيد صريح، رقم تذكرة) — لأنه دليل مباشر من كلام العميل لا تخمين.
    **قِيَم مقاسة فعلياً:** «من انت» 0.506، «نعم» 0.431، «ماهي السياسات لديكم» 0.385،
    «ممكن تفاصيل التذكرة 16» 0.473، «مش سألتك قبل شوي» 0.491 — كلها كانت تُصنَّف `complaint`
    أو `order_inquiry` وتُقاد إلى مسارات تشغيلية، وصارت `other`.
47. **Task 2 — تصعيد مقيّد (منع تكرار التذاكر):** أُضيفت في `tools.py` الدوال
    `has_explicit_escalation_request` و`is_genuine_complaint` و`should_escalate`.
    `escalate_ticket` لم تعد تُنشئ تذكرة لمجرد أن الفئة `complaint`؛ الشروط: طلب تصعيد صريح،
    أو شكوى بوصف حقيقي (عدد كلمات ≥ `MIN_COMPLAINT_WORDS = 4`)، ولا توجد تذكرة مفتوحة سابقاً
    في نفس الجلسة. عند التخطّي يُكتب `{"created": false, "skipped": true, "reason": ...}` في
    `tool_result` ليعرف الـ LLM أنه لم يُسجَّل شيء (تعزيز حارس عدم ادّعاء الإجراء).
    **ملاحظة:** خيار «علم من الـ LLM» الوارد في التوجيه **لم يُنفَّذ** (يتطلب نداء LLM إضافي
    بتكلفة وزمن أكبر)؛ اكتُفي بالشرطين القاطعين الآخرين، وهما يغطيان الحالات المرصودة.
48. **Task 3 — دورة حياة رقم الطلب:** الالتصاق صار **محدوداً بانتهاء** عبر
    `order_id_sticky_turns` + `MAX_STICKY_ORDER_TURNS = 1`. يُستعار الرقم دورة واحدة فقط،
    ويُمسح فوراً عند: ذكر رقم تذكرة (`extractor.extract_ticket_reference`)، أو أن تكون الفئة
    ليست `order_inquiry`/`complaint`، أو أن تكون الرسالة سؤال سياسة.
49. **Task 4 — السياسات والذاكرة القصيرة:** أُضيفت سياسة `shipping` إلى `fetch_policy`
    (كانت السياستان `return` و`refund` فقط)، وصار كشف أسئلة السياسات بالكلمات المفتاحية
    (`extractor.is_policy_question`) يُوجّهها إلى `fetch_policy` حتى لو كانت الفئة `other`.
    وأُضيف إلى `prompts.build_context` حقل **«سياق المحادثة السابقة»** (آخر 6 رسائل = آخر 3
    دورات من `chat_messages`، عبر `graph.recent_history`) مع بند يمنع الوكيل من إنكار معلومة
    قالها في الدور السابق، وسطر خاص بالثقة المنخفضة يطلب توضيحاً ودّياً.
50. **تغيير في شكل الرسم (ADR قسم 8.2):** كل الدورات تمرّ الآن بـ `extract_info` (لأنه
    الجهة الوحيدة التي تطبّق دورة حياة رقم الطلب)، والحافة الشرطية الوحيدة صارت **بعده**
    (`route_after_extract`)، وحُذفت `route_after_classify` لأنها صارت بلا استخدام.
    **هذا يضمن أن انتهاء الالتصاق يُطبَّق في كل دورة، لا في الدورات التشغيلية فقط.**
51. **Task 1/2/3/4 — تحديث `app.py`:** أُضيفت حقول التشخيص الجديدة إلى استجابة `/api/chat`
    (`intent_confidence`, `low_confidence`, `policy_question`, `order_id_sticky_turns`,
    `ticket_reference`, `ticket_id`) لتعكس عقد الحالة الجديد — وطُوبقت اختبارات الـ API معها.

## Phase 8 — تحسينات الواجهة وسجل الجلسات

52. **جدول `turn_outcomes` (إضافة مخطط):** أُضيف جدول إضافي يُسجَّل فيه لكل دورة
    `category`, `low_confidence`, `tool_outcome`, `had_error`. **السبب:** تعريف «تم الحل»
    يتطلب معرفة نتيجة الأدوات الفعلية (طلب موجود/سياسة مُجلَبة)، وهذه المعلومة لا تُستنتج
    من `chat_messages` ولا من `tickets`. بدونه كان الحكم على الجلسة سيكون تخميناً من النص.
    أُضيف إلى `schema.sql` و`TABLES_IN_CLEAR_ORDER`، و**لا يمسّ الجداول القائمة**.
    نظام التشغيل يتعامل بلطف مع غياب الجدول في قاعدة قديمة (تحذير فقط) — والأفضل تشغيل
    `python database/seed_data.py` مرة واحدة للترحيل.
53. **`db.list_chat_sessions()` + `_derive_session_status()`:** تُرجع لكل جلسة `session_id`,
    `first_message`, `last_message`, `updated_at`, `message_count`, `ticket_id`, `status`
    مرتّبة بـ `updated_at DESC`. القواعد موثّقة في ADR قسم 13.2.
54. **حالة رابعة `active` (قرار مُعلَن):** التوجيه طلب ثلاث حالات فقط (escalated/resolved/
    unresolved)، لكن محادثة ترحيبية بلا استعلام لا يصحّ وسمها «تم الحل» ولا «معلقة»؛
    فأُضيف وسم محايد «محادثة عامة». **قابل للإزالة بطلب صاحب المشروع.**
55. **`GET /api/sessions` + `order_details`:** نقطة نهاية جديدة للجلسات، وحقل `order_details`
    في استجابة `/api/chat` يُبنى من `tool_result` عند نجاح استعلام الطلب. وأُضيف
    `order_date` إلى `check_order_status` لتكتمل بيانات البطاقة.
56. **واجهة الواجهة الأمامية:** `ChatSidebar.jsx`/`.css` (شريط جانبي RTL بوسوم حالة ملوّنة
    وتمييز الجلسة النشطة وزر محادثة جديدة)، `OrderCard.jsx` (خط سير 3 خطوات للحالات
    النشطة + وسم أحمر وتنبيه لـ«ملغي»)، و`App.jsx`/`App.css` (رفع حالة الجلسة النشطة إلى
    الأعلى لتنسيق الشريط مع النافذة). و`api.js` نال `fetchSessions()`.
57. **إيقاع الكتابة والاقتراحات السريعة:** في `ChatWindow.jsx` — تأخير إظهار المؤشر 700ms،
    وأقل مدة عرض 400ms، وإخفاء متدرّج 260ms (دخول بـ keyframe وخروج بـ transition)،
    مع أربع رقائق اقتراح تُرسل مباشرة وتُخفى أثناء التوليد.
58. **تغيير تخطيط:** الخلفية الكحلية انتقلت من `.chat-page` إلى `.app-shell` (App.css)
    ليتشارك الشريط الجانبي والنافذة الإطار، وبقي سطح المحادثة فاتحاً.
59. **استمرارية بطاقة الطلب عبر إعادة التحميل (استُكملت بإذن صاحب المشروع):** لم تُترك
    كقيد معروف. أُضيف عمود `metadata TEXT` إلى `chat_messages` (سلسلة JSON)، وترحيل آمن
    في `init_db` عبر `PRAGMA table_info` + `ALTER TABLE ... ADD COLUMN` (idempotent)،
    وحفظ `{"order_details": ...}` مع رسالة الوكيل، وفكّها في `get_chat_history` وإرجاع
    `metadata` و`order_details` لكل رسالة في `/api/history/`، وتوحيد شكل رسائل السجل في
    `ChatWindow.jsx`. **تحقّق فعلي:** العمود أُضيف فعلاً لقاعدة قائمة (قبل: 5 أعمدة، بعد:
    6)، وتشغيل `init_db` مرتين لا يكرّر العمود، واختبار متصفح: البطاقة بقيت بعد إعادة
    تحميل الصفحة (`cards = 1` بعد أن كانت `0`) وبعد تبديل الجلسات. حُدِّث ADR قسم 13.3/13.4.
    **حالات الفشل:** JSON تالف في `metadata` يُتجاهل (تُعاد `None`) بدل إسقاط السجل.

## Phase 9 — إعادة التصميم البصري (Modern Luxury UI/UX)

60. **نطاق التغيير: CSS + عناصر تجميلية فقط.** لم يُمسّ أي منطق حالة، ولا معالجة رسائل،
    ولا تبديل جلسات، ولا مؤقّت الكتابة، ولا اتصال بالـ API. الملفات المعدَّلة:
    `public/index.html`، `index.css`، `App.css`، `ChatWindow.css`، `ChatSidebar.css`
    (إعادة كتابة بصرية كاملة)، و`ChatWindow.jsx` / `OrderCard.jsx` (إضافات DOM فقط).
61. **جرد تأثير مسبق (Ripple Audit) قبل التعديل:** فُحصت كل أصناف DOM المستخدمة في المنطق
    وفي فحوص المتصفح، وثُبِّتت كما هي — `order-card`، `order-pill`، `order-step`،
    `order-step.is-current .order-step-label`، `order-cancelled-alert`، `sidebar-item(.is-active)`،
    `session-badge`، `chat-chip`، `chat-bubble(-customer/-agent)`، `chat-typing(-row)`،
    `chat-form input/button`. لم يُحذف أو يُعدَّل أي صنف؛ الإضافات داخل عناصر قائمة فقط.
62. **قرار تصميمي — الترويسة:** أُبقي اسم المتجر «متجر النخبة» عنواناً رئيسياً لأن حذفه فقدان
    محتوى، مع صورة رمزية دائرية للوكيلة «سارة» والعنوان الفرعي المطلوب «خدمة العملاء الآلية
    المتطورة» ومؤشر اتصال نابض. (لم يُطلب حذف اسم المتجر، فلم يُحذف.)
63. **توافق خلفي للمتغيّرات:** رموز `--elite-*` من المرحلة 6 أُبقيت كأسماء بديلة تشير إلى
    اللوحة الجديدة، تفادياً لكسر أي ملف يعتمدها.
64. **إتاحة وحركة:** أُضيفت `@media (prefers-reduced-motion: reduce)` لإلغاء كل الحركات،
    وزر الإرسال صار أيقونة مع `aria-label="إرسال"` (كان نصاً)، والعناصر الزخرفية `aria-hidden`.

### التحقق الفعلي (Phase 9)

| الفحص | الأمر | النتيجة |
|---|---|---|
| بناء الواجهة | `npm run build` مع `CI=true` (التحذيرات تُعامل كأخطاء) | **Compiled successfully** — 0 تحذير و0 خطأ من ESLint/CRA |
| اختبارات الخلفية | `pytest backend/tests/` | **106 passed** (0 فشل) — بلا أي انحدار |
| تطبيق النظام بصرياً | فحص `getComputedStyle` على 14 عنصراً حقيقياً في Chrome | **14/14 مؤكَّدة**: الخط `Readex Pro`، حلقة الزجاج `blur`، حد `rgba(255,255,255,0.08)`، `animationName = pulse` للنقطة، تدرّج فقاعة العميل، `blur` لفقاعة الوكيل، توهّج الوسم، و`nodePulse` لعقدة المرحلة النشطة |
| تمايز اللوحة عن الخلفية | فك ترميز بكسل اللقطة عند حافة `.chat-window` | الخلفية `#090f1e` (سطوع 15) ← حد `#21283a` (سطوع 39.8) ← داخل `#0e192b` (سطوع 24)؛ وسيط السطوع داخل اللوحة **25.2** مقابل **19.5** خارجها (**+5.7**) — اللوحة الزجاجية ظاهرة فعلاً وليست كتلة مسطّحة |
| لوحة الألوان من البكسل | أخذ عينات من اللقطة | 95.3% خلفية ليلية داكنة، 1.9% بكسلات زمردية (اللون المميز حاضر)، نقاط نموذجية `#0b1d20` / `#0b121f` |
| عدم الانحدار الوظيفي | سكربت متصفح حقيقي: الجلسات + الرقائق + البطاقة + «ملغي» + محادثة جديدة + إعادة فتح | كل السلوك سليم: 4 رقائق، بطاقة طلب «قيد الشحن» بـ 3 خطوات، حالة «ملغي» مع التنبيه، وسم «تم الحل»، محادثة جديدة تُفرّغ النافذة (0 فقاعة)، وإعادة فتح الجلسة تُرجع **4 فقاعات وبطاقتَي طلب** |
| استمرارية البطاقة (Phase 8) | سكربت متصفح: إرسال ← إعادة تحميل ← تبديل جلسة | `cards = 1` قبل إعادة التحميل و**بعدها** و**بعد التبديل** — الاستمرارية سليمة على الثيم الجديد |

**الدليل البصري:** `docs/phase9_ui_screenshot.png` (1262×904).

> **ملاحظة صدق:** أداة الرؤية (vision) انتهت مهلتها مرتين عند وصف اللقطة، فلم أُعاينها
> بصرياً. لذلك اعتمدت **قياساً فعلياً** بدلاً من الوصف: `getComputedStyle` لكل عنصر
> + فك ترميز بكسل اللقطة وحساب السطوع. كل الأرقام أعلاه من قياس مباشر لا من انطباع.

## قائمة المنتجات المعتمدة (مرجع نهائي)

قائمة النوتبوك ذات الـ 15 منتجاً (تنقض قرار Phase 0 الأسبق «إلكترونيات — 12 منتجاً»):
سماعة بلوتوث، شاحن سريع، ساعة ذكية، جوال سامسونج، لابتوب ديل، طقم مطبخ، مكواة بخار، مكنسة كهربائية، كرسي مكتب، طاولة قهوة، حذاء رياضي، شنطة ظهر، نظارة شمسية، عطر رجالي، بطانية شتوية.

## نتائج Phase 4 الفعلية (6 سيناريوهات، نموذج + قاعدة بيانات + LLM حقيقيون)

| # | السيناريو | الفئة (الثقة) | النتيجة |
|---|---|---|---|
| 1 | استفسار عن طلب موجود 1002 | `order_inquiry` (0.997) | حالة «قيد الشحن»، تسليم 2026-09-08، وردّ صحيح يذكرهما |
| 2 | استفسار عن طلب غير موجود 987654 | `order_inquiry` (0.967) | `found=false`، والرد لم يختلق معلومة وطلب تأكيد الرقم ✔ |
| 3 | طلب إرجاع | `return_request` (0.960) | جلبت سياستي `return` (14 يوم) و `refund` (7 أيام) وشرحتهما ✔ |
| 4 | شكوى بلا رقم طلب | `complaint` (0.997) | تذكرة `ticket_id=3` بـ `order_id=None`، والرد ذكر رقم التذكرة |
| 5 | سؤال عام | `other` (0.869) | `tool_result=None` بلا تصعيد، والرد لم يختلق سياسة توصيل ✔ |
| 6 | **شكوى تذكر رقم الطلب 1002** | `complaint` (0.941) | تذكرة `ticket_id=4` بـ **`order_id=1002`** ← التصحيح يعمل ✔ |

## نتائج Phase 5 الفعلية (اختبار HTTP حقيقي — 3 رسائل متتالية بنفس `session_id`)

### أ) قبل إصلاح الذاكرة (نفس الجلسة)

| # | الرسالة | الفئة | order_id | ملاحظة |
|---|---|---|---|---|
| 1 | وين وصل طلبي رقم 1002 وهو شاحن سريع؟ | `order_inquiry` | 1002 | ردّ صحيح بالحالة والتاريخ ✔ |
| 2 | طيب متى يوصل بالضبط؟ | `order_inquiry` | **null** | **طلب رقم الطلب من جديد — لم يتذكر 1002 ✘** |
| 3 | شكرا لك، وبخصوص الإرجاع كم المدة المسموحة؟ | **`other`** | null | صُنِّفت خطأً فلم تُستدعَ السياسة ✘ |

### ب) بعد الإصلاح (جلسة جديدة `mem-fix-test`) — **الإصلاح مُتحقَّق منه**

| # | الرسالة | الفئة | order_id | ملاحظة |
|---|---|---|---|---|
| 1 | وين وصل طلبي رقم 1002 وهو شاحن سريع؟ | `order_inquiry` | 1002 | حالة «قيد الشحن» + تاريخ 2026-09-08 ✔ |
| 2 | طيب متى يوصل بالضبط؟ | `order_inquiry` | **1002** | **استُرجع الرقم من الذاكرة بلا إعادة ذكره، والرد ذكر 1002 وحالته وتاريخه ✔** |
| 3 | شكرا لك، وبخصوص الإرجاع كم المدة المسموحة؟ | `other` | 1002 | ما زالت مُصنَّفة خطأً (قيد مقبول) — لكن انظر القضية #2: **الرد ادّعى «14 يوم» بلا استعلام فعلي** ⚠ |

**سجل العقد الفعلي (إثبات الآلية):**
```
[run_turn]     input={"session_id": "sticky-1", "turn": "first"}     output={"restored_from_memory": false}
[extract_info] input={"customer_message": "وين وصل طلبي رقم 1002 ..."} output={"order_id": 1002, "order_id_sticky": false}
[run_turn]     input={"session_id": "sticky-1", "turn": "follow_up"} output={"restored_from_memory": true}
[extract_info] input={"customer_message": "طيب متى يوصل بالضبط؟"}     output={"order_id": 1002, "order_id_sticky": true}
```

**ما تحقّق فعلياً:**
- `chat_messages`: 6 صفوف بالترتيب الصحيح، وعزل تام بين الجلسات.
- الـ checkpointer: **16 checkpoint** للجلسة في `thread_id` مستقل.
- مسارات الخطأ: 400 للمدخلات، 500 لغياب المفتاح، 502 لفشل المزوّد.
- **الإصلاح الجذري:** الدورات اللاحقة تُمرِّر `session_id` + `customer_message` فقط، ويُستَرجع الباقي من `SqliteSaver`؛ و`order_id` لا يُطمس بـ `None` إذا لم تذكر الرسالة الجديدة رقماً؛ وتُصفَّر `tool_result`/`needs_escalation` كل دورة لمنع التسرّب.

## قضايا مفتوحة / مخاطر

1. ~~**[حرجة] الذاكرة لا تُستهلك وظيفياً**~~ — **أُصلحت ومُتحقَّق منها فعلياً**: `initial_state` تُستخدم للدورة الأولى فقط، والدورات اللاحقة تستخدم `turn_input` (حقلان) فيُستَرجع الباقي من الذاكرة؛ مع «`order_id` ملتصق» وتصفير الحقول لكل دورة. لم تُنفَّذ إضافة سجل المحادثة الكامل لسياق الـ LLM (قرار صاحب المشروع — خارج النطاق).
2. **[أُصلح ومُتحقَّق منه في Phase 5] اختلاق سياسات عند سوء التصنيف**: كان الرد يذكر «14 يوم» بلا أي سياسة في السياق (لأن التصنيف الخاطئ منع استدعاء `check_policy`). **الحل:** حظر صريح في `SYSTEM_PROMPT` لذكر أي رقم/مدة/تاريخ/تفصيل سياسة إلا إذا ورد حرفياً في `tool_result`، + مثال رفض ثالث. **أُعيد الاختبار فعلياً على نفس الرسالة: الرد صار يعترف بغياب المعلومة ولا يذكر أي رقم** (لا يوجد رقم واحد في الرد — مُتحقَّق منه بـ grep). **يبقى سوء التصنيف نفسه بند قياس في Phase 7**، وهذا الحارس مستقل عنه.
3. **انحراف اللهجة — أُصلح ومُتحقَّق منه**: سطر منع صريح + مثالان قصيران يمنيان في `SYSTEM_PROMPT`. **`temperature` لم يُغيَّر.** **ثبات اللهجة عبر جلسات متعددة لم يُقس بعد — Phase 7.**
4. **قيم فعلية في الأمثلة القصيرة**: معتمد من صاحب المشروع.
5. **الأدوات لا تتحقق من ملكية الطلب للعميل**: أي `order_id` مذكور يُستعلم عنه بغض النظر عن صاحبه (متوافق مع ADR).
6. **`product_name` غير ملتصق** (بعكس `order_id`): في الدورة الثانية يصبح `None` إن لم تذكر الرسالة منتجاً. هذا حسب القرار المعتمد، لكنه عدم تناظر مقصود يستحق المراجعة لاحقاً.
7. **قيد التطبيع على الأرقام**: `normalize_arabic` تطوي أي رمز مكرر 3 مرات فأكثر **بما فيها الأرقام** (`999999` → `9`). موثّق في `test_normalizer_collapses_repeated_digits_limit`.
8. **قيد استخراج رقم الطلب**: أي رقم (3–6 خانات) يلي كلمة مفتاحية مثل «رقم» يُعتبر رقم طلب. موثّق ومقبول.
9. **قيد مطابقة المنتج**: قد تخطئ عند ذكر كلمة شائعة فقط («سريع» في «توصيل سريع»). موثّق ومقبول.
10. **`expected_delivery` البذور في الماضي** بالنسبة لتاريخ اليوم (2026-09-11) لبعض الطلبات الموصولة — لا يضر العرض.
11. **عدم تطابق إصدار scikit-learn**: `label_encoder.pkl` بُني بـ 1.6.1 والمحلي 1.9.1 → تحذير فقط.
12. **مخاطرة Phase 6 (Create React App + Node 24) — لم تتحقق**: `npm install` و`npm run build` و`npm start` كلها نجحت على Node 24 / react-scripts 5.0.1 (تحذير deprecation واحد فقط: `fs.F_OK`). CRA يبقى مهجوراً كملاحظة صيانة مستقبلية، لكنه عامل الآن.
13. **تسلسل المعرّفات (`sqlite_sequence`) لا يُعاد تصفيره بالبذور**، فأرقام التذاكر الجديدة تبدأ من حيث انتهت آخر تذكرة. لا أثر وظيفي.
14. **سجل stdout لسيرفر Flask غير مُلتقَط** عند تشغيله كمهمة خلفية في هذه البيئة (ملف السجل يخرج فارغاً)، لذا اعتُمدت استجابات HTTP + مخرجات الاختبارات كدليل بدل سجل السيرفر.
15. **[أُصلح ومُتحقَّق منه] ادّعاء إجراء لم يحدث**: كان الرد في السيناريو 10 يقول «وسجّلت ملاحظتك عن التأخير» بلا إنشاء تذكرة. **الإصلاح (معتمد):** توسيع قاعدة عدم الاختراع في `SYSTEM_PROMPT` لتشمل **الأفعال** (لا يُدّعى تسجيل/حفظ/رفع/تحويل أي شيء إلا إذا أظهر `tool_result` نجاح الإجراء مثل `created=true`)، مع عرض الإجراء بدل ادّعائه. **واكتُشف تناقض داخلي** في نفس البرومبت: بند «إذا كانت الرسالة شكوى… أخبر العميل أنك سجّلت الموضوع» كان يأمر بادّعاء التسجيل بلا شرط — فأُعيدت صياغته مشروطاً بإنشاء تذكرة فعلية. **إعادة تشغيل فعلية: `action_claim_hits = []` والرد صار يعرض المتابعة** («تحب تعطيني رقم الطلب وأنا أتابعه لك»). حُدِّث ADR قسم 9.
16. **[أُصلح ومُتحقَّق منه] انحراف لهجي («نورت»)**: أُضيفت «نورت» إلى قائمة المنع في `SYSTEM_PROMPT` و ADR قسم 9 وإلى الفحص الآلي. **إعادة تشغيل فعلية للسيناريو 5: `dialect_hits = []`**. واختُبر الفحص ذاتياً على نص معروف فيلتقطها (فلا يمكن تفسير النتيجة النظيفة بأن الفحص معطّل).
17. **`docs/phase7_e2e_report.md` و`docs/PROJECT_REPORT.md`** ملفان إضافيان غير مذكورين في ADR قسم 2 — أُنشئا لتلبية متطلب قسم 10 (توثيق السيناريوهات + تقرير نهائي).

## سجل التحديثات

- 2026-09-11 — إنشاء الملف.
- 2026-09-11 — اكتمال Phase 0 + commit/push `746929e`.
- 2026-09-11 — اكتمال تحقق Phase 1.
- 2026-09-11 — اكتمال Phase 2 + commit/push `d70ed23`.
- 2026-09-11 — اكتمال Phase 3 + commit/push `42e2c40`.
- 2026-09-11 — اكتمال Phase 4: 45/45 اختبار، 6 سيناريوهات E2E بردود LLM حقيقية، إصلاح مسار الشكوى، وضبط حلّ المسارات في `config.py`.
- 2026-09-11 — Phase 4: إصلاح انحراف اللهجة (تعليمات صريحة + أمثلة يمنية)، إعادة اختبار السيناريوهات 1/3/4/5 بنجاح، وقاعدة البيانات أُعيدت نظيفة. commit/push `[Phase 4]` (`1331c3e`).
- 2026-09-11 — اكتمال Phase 5: `POST /api/chat` + `/health` + CORS، 18 اختبار API (المجموع 63/63)، اختبار HTTP حقيقي بـ 3 رسائل متتالية. اكتُشفت فجوة «الذاكرة مُخزَّنة لكن غير مُستهلكة» + تصنيف خاطئ في رسالة مختلطة.
- 2026-09-11 — Phase 5: إصلاح استهلاك الذاكرة (خيار 2+3)، 5 اختبارات جديدة (المجموع **68/68**)، وإعادة الاختبار الحقيقي تؤكد استرجاع `order_id=1002` في الدورة الثانية بلا تكرار.
- 2026-09-11 — Phase 5: حظر اختلاق الأرقام/المدد في `SYSTEM_PROMPT` + مثال رفض ثالث؛ إعادة اختبار الرسالة الثالثة تؤكد الاعتراف بغياب المعلومة بلا أي رقم (مُتحقَّق بـ grep). توثيق سلوك الحالة في ADR 8.1/8.2. commit/push `[Phase 5]`.
- 2026-09-11 — Phase 6: `GET /api/history/<session_id>` + واجهة React كاملة، 71/71 اختبار، **اختبار واجهة حقيقي في Chrome** (4 رسائل ← 8 فقاعات، وإعادة التحميل تعيد السجل كاملاً) ولقطة شاشة في `docs/`. اكتُشف وأُصلح سباق زمني في تحميل السجل. commit/push `[Phase 6]` (`fe61d2d`).
- 2026-09-11 — Phase 6 (مراجعة صاحب المشروع): حظر تنسيق Markdown في `SYSTEM_PROMPT` (تحقق فعلي: 0 من 8 ردود تحتوي أي رمز Markdown)، وإعادة تصميم بصري كحي/ذهبي (CSS فقط، بلا مكتبات). لقطة جديدة + إعادة اختبار الواجهة بنجاح. commit/push.
- 2026-09-11 — Phase 6 (ملاحظة صاحب المشروع على لقطة الشاشة): تشخيص مبني على فك ترميز بكسلات اللقطة (لا على وصف أداة الرؤية): لم يكن خللاً في الأنماط بل في التوازن البصري. الإصلاح: خلفية فاتحة صريحة لمنطقة الرسائل + نقل التدرّج المتحرك داخلها + تضييق الفقاعات. القياس بعد الإصلاح: 47.5% فاتح / 50.7% غامق. لقطة جديدة + إعادة اختبار الواجهة. commit/push `[Phase 6]`. **اعتمد صاحب المشروع التصميم نهائياً.**
- 2026-09-11 — **اكتمال Phase 7 والمشروع كاملاً**: 10/10 سيناريوهات E2E بنموذج وقاعدة بيانات و LLM حقيقيين، 71/71 اختبار وحدة، تقرير E2E + تقرير المشروع النهائي + README نهائي. اكتُشفت ملاحظتان صادقتان (ادّعاء إجراء لم يحدث، و«نورت» اللهجية) موثّقتان بلا إخفاء. commit/push `[Phase 7]` (`180ce3d`).
- 2026-09-11 — **الإغلاق النهائي**: توسيع حارس عدم الاختراع ليشمل الأفعال (+ إصلاح تناقض داخلي في بند الشكوى)، وإضافة «نورت» لقائمة المنع في البرومبت والفحص الآلي مع فحص ذاتي للمُكتشِف. أُعيد تشغيل السيناريوهين 5 و10 فعلياً: صفر ادّعاءات أفعال وصفر كلمات ممنوعة. حُدِّث ADR قسم 9 و`PROGRESS.md` وتقارير Phase 7. commit/push `[Phase 7]` (الإغلاق). **المشروع مكتمل.**
- 2026-09-11 — **سكربت التشغيل `run_project.bat`**: بُني واختُبر فعلياً — وضع `/dryrun` نظيف (كل الفحوصات تعمل)، والتشغيل الكامل شغّل الخدمتين فعلاً (health = ok وواجهة 200 والمنفذان 5000/3000)، ثم أُوقفت الخدمات وحُرّرت المنافذ. حُدِّث README بقسم التشغيل بضغطة واحدة. (كما أُزيل ملف `package-lock.json` دخيل من جذر المشروع نتج عن خطأ في طريقة تشغيل اختباراتي، ولم يُرفع.)
- 2026-09-11 — **إصلاح المعمار (Tasks 1-4)**: عتبة ثقة النية 0.65 مع تحويل ما دونها إلى `other`، وتصعيد مقيّد يمنع التذاكر الزائفة، والتصاق محدود بانتهاء لرقم الطلب، وتوجيه أسئلة السياسات (بما فيها الشحن) مع ذاكرة محادثة قصيرة في السياق. **93/93 اختبار ناجح** (71 سابق + 22 جديد). **إعادة تشغيل حيّة للتسلسل الفاشل: 10/10 فحوصات ناجحة و0 تذاكر مُنشأة** (كانت 5 تذاكر في 11 دورة). حُدِّث ADR قسم 8.1/8.2/9. commit/push `[Architecture Fix]` (`82ef164`).
- 2026-09-11 — **Phase 8 (تحسينات الواجهة وسجل الجلسات):** `GET /api/sessions` + جدول `turn_outcomes` + حقل `order_details`؛ شريط جانبي لسجل الجلسات بوسوم حالة ملوّنة؛ بطاقة طلب تفاعلية بالحالات الأربع؛ إيقاع كتابة طبيعي (تأخير 700ms، أقل عرض 400ms، إخفاء 260ms)؛ أربع رقائق اقتراح. **102/102 اختبار ناجح** (93 سابق + 9 جديد)، وبناء الواجهة **Compiled successfully**، واختبار متصفح حقيقي: الشريط الجانبي + الرقائق + بطاقة طلب («قيد الشحن» مع خط سير 3 خطوات) + حالة «ملغي» مع التنبيه + محادثة جديدة تُفرّغ النافذة + إعادة فتح جلسة (4 فقاعات). **الحالات الأربع مُتحقَّق منها فعلياً في `/api/sessions`** (`resolved`/`escalated`/`unresolved`/`active`).
- 2026-09-11 — **Phase 8 (إغلاق) — استمرارية بطاقة الطلب:** أُضيف عمود `chat_messages.metadata`
  وترحيل آمن في `init_db`، وحُفظت `order_details` مع رسالة الوكيل وفُكّت في `/api/history/`.
  **106/106 اختبار ناجح** (102 سابق + 4 جديد). **تحقّق فعلي:** الترحيل أضاف العمود لقاعدة
  قائمة (5 → 6 أعمدة) وهو idempotent، واختبار متصفح حقيقي: بطاقة الطلب بقيت بعد إعادة
  تحميل الصفحة (`cards = 1`، كانت `0`) وبعد تبديل الجلسات. **الانتظار لإذن صاحب المشروع قبل أي commit.**
- 2026-09-11 — **Phase 9 (إعادة التصميم البصري):** ثيم Midnight داكن + Glassmorphism، خط `Readex Pro`،
  شريط تمرير رقيق شبه شفاف، تدرّجات محيطية، فقاعات متدرّجة/زجاجية مع دخول `slideUpFade`،
  كبسولة كتابة بنقاط نابضة وهالة، رقائق اقتراح برفع `translateY(-2px)`، ودجت تتبّع طلب تنفيذي
  (مسار متوهّج + عقد دائرية + علامة ✓ + `nodePulse`) وحالة «ملغي» بزجاج قرمزي، ووسوم حالة متوهّجة.
  **CSS + عناصر تجميلية فقط** — لا تغيير في المنطق ولا العقود ولا مفاتيح الحمولة.
  **106/106 اختبار خلفية ناجح**، و`npm run build` مع `CI=true` → **Compiled successfully** بلا تحذيرات،
  وفحص `getComputedStyle` **14/14 مؤكَّد**، وقياس بكسل اللقطة يُثبت تمايز اللوحة عن الخلفية (+5.7 سطوعاً)،
  واختبار متصفح حقيقي يُثبت عدم الانحدار (بطاقة الطلب + الوسوم + الرقائق + جلسة جديدة + استمرارية البطاقة
  بعد إعادة التحميل والتبديل). **الانتظار لإذن صاحب المشروع قبل أي commit.**
