# سجل تقدم التنفيذ (PROGRESS)

مرجع التنفيذ: `docs/ADR.md` (قسم 10 للمراحل، قسم 11 للتوثيق).

## حالة المراحل

- [x] Phase 0 — الإعداد الأساسي (Bootstrap) — 2026-09-11 — هيكل كامل + بيئة Python 3.12 + تثبيت الاعتماديات + فحص الاستيراد/health + commit `746929e` مدفوع إلى origin/main.
- [x] Phase 1 — توليد وتنظيف الداتا سيت — 2026-09-11 — الداتا سيت (4000 صف) مُولَّد مسبقاً على Colab بواسطة صاحب المشروع، وتم **التحقق الفعلي** من معايير الجودة (Gate 1 PASS، Gate 2 PASS، مراجعة يدوية 100 صف) وإنتاج `dataset_summary.md` و `generation_log.md`.
- [x] Phase 2 — تدريب وتقييم نموذج RNN — 2026-09-11 — اعتماد مخرجات التدريب الموجودة على Colab (بدون إعادة تدريب). النموذج المعتمد `BiLSTM_Baseline` (Accuracy 0.9450، Macro F1 0.9449)، وثّق المقارنة في `model_comparison.md`، ونجح اختبار التحميل والـ Sanity Check فعلياً (5/5 اختبارات). **بانتظار إذن صريح للانتقال إلى Phase 3.**
- [ ] Phase 3 — قاعدة البيانات
- [ ] Phase 4 — طبقة الوكيل (Extractor + Tools + Graph)
- [ ] Phase 5 — Flask Backend
- [ ] Phase 6 — React Frontend
- [ ] Phase 7 — اختبار شامل وتوثيق نهائي

## قرارات إضافية أثناء التنفيذ

1. **بيئة Python 3.12 بدل 3.14**: النظام الافتراضي فيه Python 3.14.7، لكن `tensorflow` لا يوفر أي حزمة لإصدار 3.14. أُنشئت البيئة `.venv` عبر `py -3.12` (TensorFlow 2.21.0).
2. **تثبيت إصدارات الاعتماديات** في `backend/requirements.txt` على النسخ المثبّتة فعلياً.
3. **`backend/requirements.txt` ASCII فقط**: pip يفك ترميز ملف المتطلبات بترميز النظام (cp1252)، فتعليق عربي داخل الملف أسقط التثبيت بـ `UnicodeDecodeError`.
4. **`backend/config.py`**: لا قيمة افتراضية لـ `COMMANDCODE_API_KEY`؛ `require_api_key()` يرفع `RuntimeError` واضحاً عند غيابه.
5. **نقل `ADR.md` إلى `docs/ADR.md`** حسب قسم 2.
6. **الواجهة الأمامية Create React App** (`react-scripts` 5.0.1) لأن ADR يستخدم `REACT_APP_API_URL`.
7. **Phase 1 — النوتبوك مصدر الحقيقة**: لا اعتماد على أي داتا سيت إنجليزي خارجي (يلغي وصف قسم 5.1)، والتوليد يتم بالـ LLM من قوائم النوتبوك.
8. **Phase 1 — التحقق لا التوليد**: لم يُعَد توليد الداتا، واقتصر العمل على تشغيل فحوصات الجودة فعلياً.
9. **Phase 1 — سكربت التحقق** نُفِّذ من مجلد الـ scratchpad وليس كملف مشروع، التزاماً بمبدأ أقل عدد ملفات.
10. **Phase 2 — اعتماد المخرجات الجاهزة دون إعادة تدريب** (قرار صاحب المشروع).
11. **Phase 2 — استكمال `backend/agent/normalizer.py` مبكراً**: كان مطلوباً لتنفيذ اختبار التطابق/الـ Sanity Check الخاص بـ Phase 2 (ADR 6.1)، مع أن جدوله الأصلي في Phase 4. نُسخ منطق `normalize_arabic` حرفياً من النوتبوك.
12. **Phase 2 — إضافة `MODEL_CONFIG_PATH`**: النوتبوك ينتج `config.json` (يحتوي `max_len` و`vocab_size` وترتيب الفئات) وهو لازم للاستدلال. أُضيف متغير بيئة رابع بنفس نمط بقية المسارات بدل ترميز `max_len` داخل الكود (تفادياً لفشل صامت عند إعادة التدريب). حُدِّث `.env.example` و قسم 2/3 من ADR.
13. **Phase 2 — توثيق `model_comparison.md`**: قسم 10 يطلبه بصيغة `.md`، والنوتبوك ينتج `.csv`؛ حُوِّل الجدول مع كتابة مبرر الاختيار.
14. **تحديث ADR (بموافقة صاحب المشروع)**: صيغة الموديل المعتمدة `model.keras` + `config.json` (وليست `.h5`) في الأقسام 2 و 3 و 6.1، مع توثيق انقضاء قرار قائمة المنتجات الأسبق في القسم 12.

## قائمة المنتجات المعتمدة (مرجع نهائي)

بموافقة صاحب المشروع، **قائمة النوتبوك ذات الـ 15 منتجاً هي المرجع النهائي**، وتنقض قرار Phase 0 الأسبق («إلكترونيات — 12 منتجاً»):
سماعة بلوتوث، شاحن سريع، ساعة ذكية، جوال سامسونج، لابتوب ديل، طقم مطبخ، مكواة بخار، مكنسة كهربائية، كرسي مكتب، طاولة قهوة، حذاء رياضي، شنطة ظهر، نظارة شمسية، عطر رجالي، بطانية شتوية.
تُستخدم كما هي في بيانات البذور (Phase 3) وتطابق أعمدة `product_name` في الداتا سيت.

## قضايا مفتوحة / مخاطر

1. **فجوة في ADR (حرجة، لم تُحسم بعد)**: قسم 8.2 يحيل إلى «الرسم بالنقاش السابق» لتسلسل عقد LangGraph، والرسم غير مضمّن في الوثيقة. تسلسل العقد/الحواف غير معرّف — **مطلوب قبل Phase 4**.
2. **مزوّد الـ LLM**: الـ endpoint والموديل الحقيقي غير محددين صراحةً في ADR؛ النوتبوك يستخدم `deepseek/deepseek-v4.1-flash` على `https://api.commandcode.ai/provider/v1/chat/completions`. يحتاج تأكيداً قبل Phase 4/5.
3. **عدم تطابق إصدار scikit-learn**: `label_encoder.pkl` بُني بـ scikit-learn 1.6.1 (Colab) بينما المحلي 1.9.1 → `InconsistentVersionWarning` عند التحميل. الاختبارات نجحت بلا أثر ظاهر، لكنه خطر إعادة إنتاج يجب متابعته.
4. **مخاطرة توافق TensorFlow/Keras**: التحميل نجح محلياً (TF 2.21 / Keras 3 على Python 3.12) من مخرجات Colab.
5. **ثقة منخفضة نسبياً**: جملة «ابغى استرجع المنتج لان مو مطابق للمواصفات» صُنِّفت `return_request` بثقة 0.64 فقط — أدنى قيم Sanity Check، جديرة بالمراقبة في Phase 7.
6. **فجوة قسم 5.4**: لا يوجد فحص أداء على مجموعة الاختبار مقابل تنبؤات `rnn_classifier` المحلية (النموذج نفسه لم يُعَد تقييمه محلياً، اعتمدنا تقرير Colab). يمكن إضافته إن رغب صاحب المشروع.
7. **مخاطرة Phase 6**: Create React App مهجور وقد يتعارض مع Node 24.

## سجل التحديثات

- 2026-09-11 — إنشاء الملف.
- 2026-09-11 — اكتمال Phase 0 + commit/push `746929e`.
- 2026-09-11 — اكتمال تحقق Phase 1 (Gate 1 PASS, Gate 2 PASS, مراجعة يدوية 100 صف).
- 2026-09-11 — اكتمال Phase 2: اعتماد `BiLSTM_Baseline`، نجاح Sanity Check (5/5)، تحديث ADR لقائمة المنتجات وصيغة `.keras`. بانتظار الإذن للانتقال إلى Phase 3.
